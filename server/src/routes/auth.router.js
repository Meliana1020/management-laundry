const express = require('express');
const router = express.Router();

router.post('/registrasi', );
router.post('/login', );
router.get('/get-user', );
router.put('/update-user', );
router.put('/update/user-by/:id', );
router.delete('/delete/user-by/:id', );